import numpy as np
#matriz_desde_lista = np.array(lista)
#z = np.radians(z_grados)
matriz = np.array([[1, 2], [3, 4]])
transpuesta = matriz.T
inversa = np.linalg.inv(matriz)
identidad = np.identity(2)